{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "debug",
            "type": "lldb",
            "request": "launch",
            "program": "./zig-out/bin/${workspaceFolderBasename}",
            "MIMode": "lldb",
            "webRoot": "${workspaceRoot}",
            "preLaunchTask": "build"
        }
    ]
}